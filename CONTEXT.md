# Marginal（跨端 AI 电子书应用）

个人向的跨端电子书应用：导入本地 TXT/EPUB 小说，用 AI 修复结构与内容，建立人物/场景实体卡并为段落生成插图；目标平台 H5/iOS/Android，客户端内置 Agent 运行时。

## Language

### 文本

**书稿 (Work)**:
导入的一本小说及其全部派生数据（章节、修订、实体卡、插图）的根实体。
_Avoid_: 书、项目

**章节 (Chapter)**:
书稿按切分结果划分的阅读单元，是修复与插图操作的上下文。
_Avoid_: 卷、节

**修复 (Repair)**:
对导入文本的章节切分与内容清洗的总称。
_Avoid_: 清洗、纠错

**修订 (Revision)**:
AI 或用户对文本产生的一次可回滚的变更记录。
_Avoid_: 改动、补丁

**修复批次 (Repair Run)**:
一次执行修复流水线的任务单元；其产生的全部修订共享批次号，支持整批回滚。
_Avoid_: 任务、轮次

**锚点 (Anchor)**:
在文本中以相对位置表示的引用点，供插图插入并跨重排存活。
_Avoid_: 位置、引用

### AI

**Agent 运行时 (Agent Runtime)**:
客户端内执行的多轮推理循环：根据目标选择已注册工具、执行、观察结果并继续行动，直至给出最终结果；工具权限受客户端策略约束。
_Avoid_: Pi、MCP、聊天机器人

**Agent 工具 (Agent Tool)**:
Agent 运行时可调用的具名能力，按风险分为只读与写入两级；写入工具的产物必须经用户确认后才生效。
_Avoid_: 插件、函数

**提案 (Proposal)**:
Agent 或流水线产生的待确认变更；批准后落为修订或相应实体，拒绝即丢弃；同一 Agent 会话的已批准变更共享批次并支持整批回滚。
_Avoid_: 草稿、建议

**Agent 会话 (Agent Run)**:
一次 Agent 运行时执行的完整单元：绑定单本书稿，记录轮次、工具调用、用量与产生的提案；作为批次计费与回滚的边界。
_Avoid_: 任务、对话

**工具调用记录 (Tool Call)**:
Agent 会话中一次工具执行的审计记录，包含工具名、输入摘要、结果摘要与批准状态。
_Avoid_: 调用栈、普通日志

**任务类型 (Task Kind)**:
AI 调用的用途分类：修复、实体提取、插图生成；每类独立配置供应商与模型。
_Avoid_: 模型配置、渠道

**供应商 (Provider)**:
用户自行配置的模型服务接入点（端点、凭据、模型与能力）；首个支持协议为 OpenAI-compatible，架构上不锁定单一协议。
_Avoid_: 厂商、API、中转

**实体卡 (Entity Card)**:
从文本提取的人物、场景、物品等描述卡片，是插图的描述依据。
_Avoid_: 人物库、设定集、词条

**正典 (Canon)**:
实体卡被用户确认后的状态；正典卡是插图链路提供参考图的资格来源。
_Avoid_: 锁定、批准

**定妆照 (Portrait)**:
正典实体卡的标准参考图，是参考图资格的具体载体；AI 生成或用户上传。
_Avoid_: 头像、封面

### 产品表面

**书稿工具菜单 (Work Actions)**:
作用于当前书稿的操作入口；包含继续阅读、目录、收藏、分组、重命名、删除、全书包导入/导出，以及 AI 修复、实体卡、插图和分享入口。它与“我的”中的全局设置分离。
_Avoid_: 全局设置、项目设置

**全局设置 (Global Preferences)**:
作用于应用或默认阅读体验的设置，归属于“我的”Tab；包括阅读主题、字号/行距、自动阅读默认值、数据管理与供应商配置。
_Avoid_: 书稿工具菜单

**书库 Tab (Library Tab)**:
移动端一级导航中的书稿工作区，负责继续阅读、书稿列表、搜索、筛选、分组、收藏、导入与书稿工具菜单。

**超长章节 (Long Chapter)**:
正文长度超过常规整章渲染安全范围的章节；阅读器必须以分块/虚拟化方式呈现，使其不因全文一次构建而阻断阅读。章节结构的修改仍通过修复提案确认，不由阅读器自动改写。
_Avoid_: 阅读失败、自动拆章

**AI Tab (AI Tab)**:
移动端一级导航中的 AI 工作区，负责供应商状态、连接诊断、修复批次、提案、Agent 会话、实体卡与插图任务；不承担首页的核心阅读入口。

**我的 Tab (Me Tab)**:
移动端一级导航中的全局设置工作区，不承载单本书稿的操作。

### 同步

**全书包 (Book Bundle)**:
单本作品的自包含导出文件（文本、修订、实体卡、插图），用于双端手动同步。
_Avoid_: 备份、存档、导出文件

### 阅读呈现

**语义正文 (Semantic Text)**:
EPUB 导入时抽取出的纯文本正文；是跨平台默认阅读形态，承载窗口化 I/O、段落交互、收藏与自动阅读。
_Avoid_: 纯文本模式、降级文本

**原版排版 (Original Layout)**:
按 EPUB 的原始 XHTML/CSS/字体/固定版式 viewport 渲染章节的模式；仅在浏览器目标可用，资源以内嵌 data URI 离线加载，EPUB 脚本被禁止。
_Avoid_: 富文本模式、WebView 阅读

**渲染资源 (Render Resources)**:
EPUB 压缩包内按原始路径保存的 XHTML、CSS、图片、SVG、字体 Blob；章节通过 `epub-source` 锚点指向其原始 XHTML。
_Avoid_: 附件、素材
