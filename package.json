{
  "name": "marginal",
  "private": true,
  "version": "2.0.0",
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "build:web": "cd apps/marginal && flutter build web --release --no-wasm-dry-run && rm -rf web-dist && cp -R build/web web-dist",
    "test:flutter": "cd apps/marginal && flutter test",
    "analyze:flutter": "cd apps/marginal && flutter analyze"
  }
}
